<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://fredshone.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://fredshone.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-01T20:28:35+00:00</updated><id>https://fredshone.github.io/feed.xml</id><title type="html">Fred Shone</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Measuring the Distance Between Activity Populations</title><link href="https://fredshone.github.io/blog/2026/acteval-distances/" rel="alternate" type="text/html" title="Measuring the Distance Between Activity Populations"/><published>2026-03-29T00:00:00+00:00</published><updated>2026-03-29T00:00:00+00:00</updated><id>https://fredshone.github.io/blog/2026/acteval-distances</id><content type="html" xml:base="https://fredshone.github.io/blog/2026/acteval-distances/"><![CDATA[<h2 id="acteval">Acteval</h2> <p>This is a post to introduce key features in <a href="https://github.com/fredshone/acteval">acteval</a> - a project for measuring the difference between populations of activity schedules using density estimation.</p> <p>Activity schedules are the things we do and when. Like leave the house at 7, work from 8.30 till 5.30, then nip to the shops for 20 minutes on the way home.</p> <p>A tyical use case for acteval is to check how well you modelled or synthesised schedules match some target distribution.</p> <p>In this post we walk through a full pipeline on a pair of constructed example populations, from raw DataFrames to domain-level rankings.</p> <h2 id="what-are-activity-sequences">What are activity sequences?</h2> <p>A daily activity schedule is a sequence of non-overlapping episodes that typically span a 24-hour day. Each episode records who did it (we use person ID - <code class="language-plaintext highlighter-rouge">pid</code>), what activity they performed (<code class="language-plaintext highlighter-rouge">act</code>), and when (<code class="language-plaintext highlighter-rouge">start</code>, <code class="language-plaintext highlighter-rouge">end</code>, <code class="language-plaintext highlighter-rouge">duration</code>):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="n">acteval.describe</span> <span class="kn">import</span> <span class="n">plot</span>

<span class="n">example</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">([</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">pid</span><span class="sh">"</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">act</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">home</span><span class="sh">"</span><span class="p">,</span>    <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span>    <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">450</span><span class="p">,</span> <span class="sh">"</span><span class="s">duration</span><span class="sh">"</span><span class="p">:</span> <span class="mi">450</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">pid</span><span class="sh">"</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">act</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">work</span><span class="sh">"</span><span class="p">,</span>    <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">450</span><span class="p">,</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">810</span><span class="p">,</span> <span class="sh">"</span><span class="s">duration</span><span class="sh">"</span><span class="p">:</span> <span class="mi">360</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">pid</span><span class="sh">"</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">act</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">eat_out</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">810</span><span class="p">,</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">870</span><span class="p">,</span> <span class="sh">"</span><span class="s">duration</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">60</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">pid</span><span class="sh">"</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">act</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">work</span><span class="sh">"</span><span class="p">,</span>    <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span>  <span class="mi">870</span><span class="p">,</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1080</span><span class="p">,</span> <span class="sh">"</span><span class="s">duration</span><span class="sh">"</span><span class="p">:</span> <span class="mi">210</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">pid</span><span class="sh">"</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">act</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">home</span><span class="sh">"</span><span class="p">,</span>    <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1080</span><span class="p">,</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1440</span><span class="p">,</span> <span class="sh">"</span><span class="s">duration</span><span class="sh">"</span><span class="p">:</span> <span class="mi">360</span><span class="p">},</span>
<span class="p">])</span>

<span class="n">ACTS</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">home</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#5b8dd9</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">work</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#d95b5b</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">eat_out</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#e8a838</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">shop</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#5db85d</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">leisure</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#a05bb8</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">education</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">#5bbcbc</span><span class="sh">"</span><span class="p">,</span>
<span class="p">}</span>

<span class="n">plot</span><span class="p">.</span><span class="nf">gantt</span><span class="p">(</span>
    <span class="n">populations</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">Sample</span><span class="sh">"</span><span class="p">:</span> <span class="n">example</span><span class="p">},</span>
    <span class="n">act_colors</span><span class="o">=</span><span class="n">ACTS</span><span class="p">,</span>
    <span class="n">acts</span><span class="o">=</span><span class="n">ACTS</span><span class="p">.</span><span class="nf">keys</span><span class="p">(),</span>
<span class="p">)</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/acteval/fig1-sample-gantt.svg-480.webp 480w,/assets/img/acteval/fig1-sample-gantt.svg-800.webp 800w,/assets/img/acteval/fig1-sample-gantt.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/acteval/fig1-sample-gantt.svg" width="100%" height="auto" style=" max-width: 70%; " loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Example activity schedule.</figcaption> </figure> <p>Times are typically minutes from midnight but for now any consistent unit works; the library normalises internally.</p> <h2 id="what-sort-of-distances">What sort of distances?</h2> <p>On it’s own a schedule is a complex object. As a though experiment we can image representing a schedule as 1440 activity choices (each representing a minute of the day). If we were to represent a schedule with just two possible activity types (say <code class="language-plaintext highlighter-rouge">home</code> and <code class="language-plaintext highlighter-rouge">work</code>) then there are $2^{1440}$ possible schedules. A population is then comprised of potentially millions of person’s schedules.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="n">acteval.describe</span> <span class="kn">import</span> <span class="n">plot</span>
<span class="kn">from</span> <span class="n">acteval.scripts.generate_blog_plots</span> <span class="kn">import</span> <span class="n">generate_suburban_workers</span><span class="p">,</span> <span class="n">generate_lifestyle</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">generate_suburban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="n">plot</span><span class="p">.</span><span class="nf">gantt</span><span class="p">(</span><span class="n">populations</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="p">:</span> <span class="n">A</span><span class="p">},</span> <span class="n">act_colors</span><span class="o">=</span><span class="n">ACTS</span><span class="p">,</span> <span class="n">acts</span><span class="o">=</span><span class="n">ACTS</span><span class="p">.</span><span class="nf">keys</span><span class="p">())</span>

</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/acteval/fig2-samples-gantt.svg-480.webp 480w,/assets/img/acteval/fig2-samples-gantt.svg-800.webp 800w,/assets/img/acteval/fig2-samples-gantt.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/acteval/fig2-samples-gantt.svg" width="100%" height="auto" style=" max-width: 70%; " loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Example activity schedule.</figcaption> </figure> <p>We can think of these populations as really big complex distributions, with mixtures of discrete and continuous dimensions. Vast swathes of this theorized disribution are empty (no one <em>should</em> flip-flop hundreds of times between work and home in a day) and some are quite dense (like the classic home-work-home sequence).</p> <p>Our focus is to pick out subtle changes, including changes to pattens in activity particapations, changes to the order of activities, their durations and when they happen.</p> <p>To do this we try to measure differences in a meaningful way. Specifically, we approximate the nasty complex distribution of the population with loads and loads of meaningful marginal distributions. For example:</p> <ul> <li>how often do people participate in <code class="language-plaintext highlighter-rouge">home</code>, <code class="language-plaintext highlighter-rouge">work</code>, <code class="language-plaintext highlighter-rouge">shop</code>, and so on?</li> <li>how often do people travel from <code class="language-plaintext highlighter-rouge">work</code> to <code class="language-plaintext highlighter-rouge">shop</code>, <code class="language-plaintext highlighter-rouge">shop</code> to <code class="language-plaintext highlighter-rouge">work</code> and so on?</li> <li>when do people tend to start <code class="language-plaintext highlighter-rouge">work</code> and how long do they <code class="language-plaintext highlighter-rouge">work</code> for?</li> </ul> <p>We tend to refer to these as population features, rather than marginal distributions.</p> <h2 id="features">Features</h2> <p>We expose population features as pre-computed numpy arrays via the <code class="language-plaintext highlighter-rouge">eval.Population</code> class:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval</span> <span class="kn">import</span> <span class="n">Population</span>
<span class="kn">from</span> <span class="n">acteval.describe</span> <span class="kn">import</span> <span class="n">plot</span>

<span class="n">A</span> <span class="o">=</span> <span class="nc">Population</span><span class="p">(</span><span class="nf">generate_urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">))</span>
<span class="nf">print</span><span class="p">(</span><span class="n">A</span><span class="p">.</span><span class="n">count_matrix</span><span class="p">[</span><span class="o">-</span><span class="mi">3</span><span class="p">:])</span>
<span class="c1"># [
#  [0 1 2 1 0 0]
#  [0 0 2 0 1 1]
#  [0 0 2 0 0 1]
# ]
</span><span class="nf">print</span><span class="p">(</span><span class="n">A</span><span class="p">.</span><span class="n">int_to_act</span><span class="p">)</span>
<span class="c1"># ['eat_out' 'education' 'home' 'leisure' 'shop' 'work']
</span>
</code></pre></div></div> <hr/> <p><code class="language-plaintext highlighter-rouge">Population</code> integer-encodes activities and person IDs on construction, and lazily caches expensive derived quantities (n-gram keys, count matrices) on first access. This pays off when evaluating many synthetic models against the same observed data.</p> <h2 id="feature-distributions">Feature distributions</h2> <p>These features form distributions. Such as the numbers of activities in each plan:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval.features.participation</span> <span class="kn">import</span> <span class="n">sequence_lengths</span>
<span class="kn">from</span> <span class="n">acteval.describe</span> <span class="kn">import</span> <span class="n">plot</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">A</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">())</span>
<span class="c1"># {'sequence lengths': (array([3., 4., 5.]), array([230, 395, 375]))}
</span>
<span class="n">_</span> <span class="o">=</span> <span class="n">plot</span><span class="p">.</span><span class="nf">sequence_lengths</span><span class="p">({</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="p">:</span> <span class="n">A</span><span class="p">})</span>

</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/acteval/fig2-sequence-lengths.svg-480.webp 480w,/assets/img/acteval/fig2-sequence-lengths.svg-800.webp 800w,/assets/img/acteval/fig2-sequence-lengths.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/acteval/fig2-sequence-lengths.svg" width="100%" height="auto" style=" max-width: 60%; " loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Example population feature distributions.</figcaption> </figure> <p>Note that <code class="language-plaintext highlighter-rouge">sequence_lengths_per_pid</code> returns a <code class="language-plaintext highlighter-rouge">PidFeatures</code> object. We then use <code class="language-plaintext highlighter-rouge">aggregate</code> to extract the distribution as a tuple of counts and their frequncies. <code class="language-plaintext highlighter-rouge">PidFeatures</code> can be subset based on some sub-population of person ids to get more refined distributions. But more on this later.</p> <h2 id="feature-distances">Feature distances</h2> <p>Consider two distribution of sequence lengths, one from population <code class="language-plaintext highlighter-rouge">A</code>, the other from population <code class="language-plaintext highlighter-rouge">B</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval.features.participation</span> <span class="kn">import</span> <span class="n">sequence_lengths</span>
<span class="kn">from</span> <span class="n">acteval.describe</span> <span class="kn">import</span> <span class="n">plot</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
<span class="n">B</span> <span class="o">=</span> <span class="nf">leisure_dominant</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">A</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">())</span>
<span class="c1"># {'sequence lengths': (array([3., 4., 5.]), array([230, 395, 375]))}
</span><span class="nf">print</span><span class="p">(</span><span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">B</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">())</span>
<span class="c1"># {'sequence lengths': (array([3., 4., 5.]), array([407, 520,  73]))}
</span>
<span class="n">_</span> <span class="o">=</span> <span class="n">plot</span><span class="p">.</span><span class="nf">sequence_lengths</span><span class="p">({</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span><span class="p">:</span> <span class="n">A</span><span class="p">,</span> <span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="p">:</span> <span class="n">B</span><span class="p">})</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/acteval/fig3-sequence-lengths.svg-480.webp 480w,/assets/img/acteval/fig3-sequence-lengths.svg-800.webp 800w,/assets/img/acteval/fig3-sequence-lengths.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/acteval/fig3-sequence-lengths.svg" width="100%" height="auto" style=" max-width: 60%; " loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Example population feature distributions.</figcaption> </figure> <p>We measure the distance between these two distributions using <strong>Earth Mover’s Distance</strong> (EMD), also known as the Wasserstein distance. Informally: imagine each distribution as a pile of soil spread across a number line. The EMD is the minimum amount of work needed to rearrange one pile into the shape of the other, where work = area × distance moved.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kn">from</span> <span class="n">acteval.distance.wasserstein</span> <span class="kn">import</span> <span class="n">emd</span>
<span class="kn">from</span> <span class="n">acteval.features.participation</span> <span class="kn">import</span> <span class="n">sequence_lengths</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
<span class="n">B</span> <span class="o">=</span> <span class="nf">leisure_dominant</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="n">features_A</span> <span class="o">=</span> <span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">A</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">()</span>
<span class="n">features_B</span> <span class="o">=</span> <span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">B</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">()</span>

<span class="nf">print</span><span class="p">(</span><span class="nf">emd</span><span class="p">(</span><span class="n">features_A</span><span class="p">[</span><span class="sh">"</span><span class="s">sequence lengths</span><span class="sh">"</span><span class="p">],</span> <span class="n">features_B</span><span class="p">[</span><span class="sh">"</span><span class="s">sequence lengths</span><span class="sh">"</span><span class="p">]))</span>
<span class="c1"># 0.47899999999999987
</span>
</code></pre></div></div> <p>We like EMD because the unit of distance is often quite meaningful. For example, a distance of 1, between sequence length distributions, is equivalent to saying that a population’s schedules are typically one activitiy longer or shorter than another. But keep in mind they could also have the same expected value, but be distributed more and less flatly. To find out, a user has to plot the distribution or calculate descriptive metrics.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval.distance.wasserstein</span> <span class="kn">import</span> <span class="n">emd</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
<span class="n">B</span> <span class="o">=</span> <span class="nf">leisure_dominant</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="n">features_A</span> <span class="o">=</span> <span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">A</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">()</span>
<span class="n">features_B</span> <span class="o">=</span> <span class="nf">sequence_lengths</span><span class="p">(</span><span class="nc">Population</span><span class="p">(</span><span class="n">B</span><span class="p">)).</span><span class="nf">aggregate</span><span class="p">()</span>

<span class="n">vals_A</span><span class="p">,</span> <span class="n">counts_A</span> <span class="o">=</span> <span class="n">features_A</span><span class="p">[</span><span class="sh">"</span><span class="s">sequence lengths</span><span class="sh">"</span><span class="p">]</span>
<span class="n">vals_B</span><span class="p">,</span> <span class="n">counts_B</span> <span class="o">=</span> <span class="n">features_B</span><span class="p">[</span><span class="sh">"</span><span class="s">sequence lengths</span><span class="sh">"</span><span class="p">]</span>

<span class="n">mean_A</span> <span class="o">=</span> <span class="p">(</span><span class="n">vals_A</span> <span class="o">*</span> <span class="n">counts_A</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">counts_A</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
<span class="n">mean_B</span> <span class="o">=</span> <span class="p">(</span><span class="n">vals_B</span> <span class="o">*</span> <span class="n">counts_B</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">counts_B</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Expected number of actvities per sequence:</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Population A: </span><span class="si">{</span><span class="n">mean_A</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Population B: </span><span class="si">{</span><span class="n">mean_B</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>In practice, <code class="language-plaintext highlighter-rouge">acteval</code> represents distributions as weighted histograms — <code class="language-plaintext highlighter-rouge">(values, weights)</code> tuples — and computes EMD via the <a href="https://pythonot.github.io/">POT</a> library.</p> <hr/> <p>Note that we are measuring the distance between populations of schedules. Compared population don’t need to be comprised of the same persons or be the same size. Comparison is not therefore pairwise. individual schedules, is being added to acteval (see the pair-wise module).</p> <h2 id="bringing-it-all-togther">Bringing it all togther</h2> <p>The <code class="language-plaintext highlighter-rouge">sequence_length</code> feature is a useful comparison, but obviously there’s a lot more going on in activity schedules. The <code class="language-plaintext highlighter-rouge">acteval</code> strategy is to simply consider loads and loads of features.</p> <p>For example, we consider the number of times each activity type occurs (<code class="language-plaintext highlighter-rouge">participation rates</code>), the number of times each transition from one activity type to another occurs (<code class="language-plaintext highlighter-rouge">2-grams</code>), the durations of each activity type (<code class="language-plaintext highlighter-rouge">durations</code>), and so on. The full catelogue, and which of these are used in the default evaluation configuration are available from <code class="language-plaintext highlighter-rouge">acteval.features.catalogue</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval.features</span> <span class="kn">import</span> <span class="n">catalogue</span>

<span class="nf">print</span><span class="p">(</span><span class="n">catalogue</span><span class="p">.</span><span class="nf">list_features</span><span class="p">().</span><span class="nf">to_markdown</span><span class="p">())</span>
</code></pre></div></div> <table> <thead> <tr> <th style="text-align: right"> </th> <th style="text-align: left">domain</th> <th style="text-align: left">group</th> <th style="text-align: left">config_key</th> <th style="text-align: left">description</th> <th style="text-align: left">in_default_config</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">0</td> <td style="text-align: left">participations</td> <td style="text-align: left">sequence lengths</td> <td style="text-align: left">lengths</td> <td style="text-align: left">Distribution of number of episodes per person.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">1</td> <td style="text-align: left">participations</td> <td style="text-align: left">participation rate</td> <td style="text-align: left">rates</td> <td style="text-align: left">How many times each person participates in each activity.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">2</td> <td style="text-align: left">participations</td> <td style="text-align: left">pair participation rate</td> <td style="text-align: left">pair_rates</td> <td style="text-align: left">Co-participation counts for all activity pairs.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">3</td> <td style="text-align: left">participations</td> <td style="text-align: left">seq participation rate</td> <td style="text-align: left">seq_rates</td> <td style="text-align: left">Participation rates keyed by sequence position (e.g. ‘0home’, ‘1work’).</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">4</td> <td style="text-align: left">participations</td> <td style="text-align: left">enum participation rate</td> <td style="text-align: left">enum_rates</td> <td style="text-align: left">Participation rates keyed by n-th occurrence of each activity (e.g. ‘home0’, ‘home1’).</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">5</td> <td style="text-align: left">timing</td> <td style="text-align: left">start times</td> <td style="text-align: left">start_times</td> <td style="text-align: left">Start-time distribution per activity × occurrence index.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">6</td> <td style="text-align: left">timing</td> <td style="text-align: left">durations</td> <td style="text-align: left">durations</td> <td style="text-align: left">Duration distribution per activity × occurrence index.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">7</td> <td style="text-align: left">timing</td> <td style="text-align: left">start-durations</td> <td style="text-align: left">start_durations</td> <td style="text-align: left">Joint (start, duration) 2-D distribution per activity.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">8</td> <td style="text-align: left">timing</td> <td style="text-align: left">joint-durations</td> <td style="text-align: left">joint_durations</td> <td style="text-align: left">Joint (duration_i, duration_{i+1}) distribution for consecutive activity pairs.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">9</td> <td style="text-align: left">timing</td> <td style="text-align: left">start times by act</td> <td style="text-align: left">start_times_by_act</td> <td style="text-align: left">Start-time distribution per activity (no occurrence index).</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">10</td> <td style="text-align: left">timing</td> <td style="text-align: left">end times by act</td> <td style="text-align: left">end_times_by_act</td> <td style="text-align: left">End-time distribution per activity (no occurrence index).</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">11</td> <td style="text-align: left">timing</td> <td style="text-align: left">durations by act</td> <td style="text-align: left">durations_by_act</td> <td style="text-align: left">Duration distribution per activity (no occurrence index).</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">12</td> <td style="text-align: left">timing</td> <td style="text-align: left">time consistency</td> <td style="text-align: left">time_consistency</td> <td style="text-align: left">Per-person flags: starts at 0, ends at 1440, total duration equals 1440.</td> <td style="text-align: left">False</td> </tr> <tr> <td style="text-align: right">13</td> <td style="text-align: left">transitions</td> <td style="text-align: left">2-gram</td> <td style="text-align: left">2-gram</td> <td style="text-align: left">Consecutive activity pair (bigram) counts per person.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">14</td> <td style="text-align: left">transitions</td> <td style="text-align: left">3-gram</td> <td style="text-align: left">3-gram</td> <td style="text-align: left">Consecutive activity triple (trigram) counts per person.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">15</td> <td style="text-align: left">transitions</td> <td style="text-align: left">4-gram</td> <td style="text-align: left">4-gram</td> <td style="text-align: left">Consecutive activity quad (4-gram) counts per person.</td> <td style="text-align: left">True</td> </tr> <tr> <td style="text-align: right">16</td> <td style="text-align: left">transitions</td> <td style="text-align: left">full sequences</td> <td style="text-align: left">full_sequences</td> <td style="text-align: left">Per-person indicator for each unique full abbreviated tour string (e.g. ‘h&gt;w&gt;h’).</td> <td style="text-align: left">False</td> </tr> </tbody> </table> <p>…Which is a lot, so to be more useful for quick comparisons, we encourage aggregations to <strong>group</strong> and <strong>domain</strong> levels. The highest level, <strong>domain</strong>, consists of the following:</p> <ul> <li><strong>participations</strong>: people taking part in activities</li> <li><strong>transitions</strong>: people moving between activities</li> <li><strong>timing</strong>: when and for how long people do things</li> </ul> <p>The <code class="language-plaintext highlighter-rouge">acteval.Evaluator</code> orchestrated all these comparisons in an efficient way. It also looks after descriptive metrics and non-density estimation features, such as for measuring correctness and creativity.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">acteval</span> <span class="kn">import</span> <span class="n">Evaluator</span>

<span class="n">A</span> <span class="o">=</span> <span class="nf">urban_workers</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
<span class="n">B</span> <span class="o">=</span> <span class="nf">leisure_dominant</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
<span class="n">C</span> <span class="o">=</span> <span class="nf">education_leaning</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>

<span class="n">evaluator</span> <span class="o">=</span> <span class="nc">Evaluator</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">A</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">evaluator</span><span class="p">.</span><span class="nf">compare</span><span class="p">({</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="p">:</span> <span class="n">B</span><span class="p">,</span> <span class="sh">"</span><span class="s">C</span><span class="sh">"</span><span class="p">:</span> <span class="n">C</span><span class="p">}))</span>

<span class="c1"># EvalResult — 2 model(s): B, C
#                        B         C
# domain                            
# creativity      0.006521  0.016128
# feasibility     0.000000  0.000000
# participations  0.263421  0.205167
# timing          0.061743  0.031365
# transitions     0.243480  0.184492
</span>
</code></pre></div></div> <h2 id="more-to-come">More to come</h2> <ul> <li>Creativity and diversity</li> <li>Attributes</li> <li>Reporting</li> <li>Pair-wise</li> </ul>]]></content><author><name></name></author><category term="Dev"/><category term="acteval,"/><category term="transport,"/><category term="evaluation"/><summary type="html"><![CDATA[A walkthrough of acteval's semantic distance pipeline: how to extract meaningful features from daily activity schedules, compute Earth Mover's Distance between populations, and aggregate the results into interpretable domain scores.]]></summary></entry><entry><title type="html">How intelligent should our models be?</title><link href="https://fredshone.github.io/blog/2026/AGI-vs-SGI-HGI/" rel="alternate" type="text/html" title="How intelligent should our models be?"/><published>2026-03-19T00:00:00+00:00</published><updated>2026-03-19T00:00:00+00:00</updated><id>https://fredshone.github.io/blog/2026/AGI-vs-SGI-HGI</id><content type="html" xml:base="https://fredshone.github.io/blog/2026/AGI-vs-SGI-HGI/"><![CDATA[<p><em>What to make of the drive for superhuman intelligence regarding the problem of modelling humans?</em></p> <hr/> <p>This post is to simply make the point that the hunt for super-general-intelligence (SGI) is tangential to what we need for modelling humans and probably systems of humans.</p> <h1 id="what-is-intelligence">What is intelligence?</h1> <p>Psychologists, biologists, and computer scientists have been arguing about the definition of intelligence for some time.</p> <p>My first, unastonishing take, is that checking the length of a tape-measure with the same tape-measure is probematic. Humans defining a thing, using that thing is self-referential or circular.</p> <p>You can escape this loop with an evolutionary inspired take on intelligence. Broadly, intelligence isn’t an abstract universal property. Instead it’s fitness-for-purpose for context.</p> <p>Therefore intelligence changes when the environment changes.</p> <h1 id="hooman-intelligence">Hooman intelligence?</h1> <p>AI has caught up with me in some ways. It has been better at me at Go for a while and now probaby writes better code than me. I expect it will be a better researcher than me given some appropriate goals.</p> <p>Surpassing humans isn’t always a high bar. Humans are genuinely bad at stuff. We’re poor at reasoning under uncertainty. We misread probabilities. We discount the future too steeply. We’re overconfident in familiar domains and panicked in unfamiliar ones. I’d argue that we’re a little over-optimised for arrising from the primordial soup. But suprisingly bad at navigating sensibly across London.</p> <h1 id="super-intelligence-and-becoming-inhuman">Super intelligence and becoming inhuman</h1> <p>Reinforcement learning (RL) is at the core of pushing past huan capability. Rather than being anchored in human observations, it can push past into uncharted challenges or context. The mechanism is elegant: give the system a reward signal, let it explore, and iterate until it finds strategies no human designer would have thought of.</p> <p>RL works best in environments with clear reward signals, well-defined rules, and computable feedback. Games. Simulations. Formal problems with ground truths.</p> <p>These are precisely the environments where humans are weakest — combinatorial, high-dimensional, fully observable. Pushing AI capability in these domains is tangential to modeling humans.</p> <h1 id="how-to-be-human">How to be human</h1> <p>The human workday doesn’t have a clean reward function. We use heuristics, habits, identity, shortcuts, and instinct. Our “objectives” operate on timescales from seconds to decades simultaneously, and they conflict with each other constantly. The reasons we give for our choices are often retrospective. We often can’t be trusted to knwo why we did something at all[1].</p> <p>This is a tricky reward (or utility[2]) function to devise, and certainly not what sits at the heart of LLMs and other models on their way to SGI[3]. Instead I suggest we simply imitate the pattern. Absorb the mess and reflect it back without needing to reduce it to a clean objective[4].</p> <hr/> <p>[1] See <a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC4172758/">choice blindness</a></p> <p>[2] There are some parallels between RL and classic behavioural based choice models. But they are out of scope here. Simply we can say that utility based approaches are also optimisers and that this is also problematic.</p> <p>[3] We could still perhaps spin of our own SGI (Sh*t General Intelligence)</p> <p>[4] This argumant is heavilly reliant on the assumption that we model humans as agent that interact to simulate collective behaviours. Rather than simply cut the corner and model the collective sytem directly, for example. Such a system would certainy need to be superhuman and I don’t have much time for it because (i) such complexity seems intractible, and (ii) how to trust a system that one-shots the future without showing us any working?</p>]]></content><author><name></name></author><category term="Dev"/><category term="ML,"/><category term="LLM"/><summary type="html"><![CDATA[Tangential: superhuman AI and why it won't help us understand people]]></summary></entry><entry><title type="html">The unreasonable difficulty of modelling reasonable people</title><link href="https://fredshone.github.io/blog/2026/transport&Ai/" rel="alternate" type="text/html" title="The unreasonable difficulty of modelling reasonable people"/><published>2026-03-03T00:00:00+00:00</published><updated>2026-03-03T00:00:00+00:00</updated><id>https://fredshone.github.io/blog/2026/transport&amp;Ai</id><content type="html" xml:base="https://fredshone.github.io/blog/2026/transport&amp;Ai/"><![CDATA[<p><em>What can language models teach us about modelling humans? And what should we be careful not to learn?</em></p> <hr/> <h1 id="a-brief-history-of-making-things-up">A brief history of making things up</h1> <p>Forty years ago, language modelling was pretty theoretical. Linguists and statisticians, working with limited data and hard-won assumptions about the structure of language, strived and mostly failed to generate realistic language (certainly compared to the state of the art today). Models were tidy, interpretable, and <a href="https://sites.google.com/view/elizaarchaeology/try-eliza">quite bad at language</a>.</p> <p>Modern data-driven approaches have <em>replaced</em> these old approaches. Transport modelling, and human behavioural modelling more generally, finds itself roughly where language modelling was forty years ago.</p> <h1 id="the-transport-modellers-predicament">The transport modeller’s predicament</h1> <p><strong><em>We have a lot of structure and not enough data.</em></strong></p> <p>Travel demand models - discrete choice, activity-based, and so on - are carefully specified. We define utility functions from the literature or based on available data, then estimate or assume the parameters. Models encode decades of behavioural theory. You can look at them and, if you are an expert, sort of understand them.</p> <p>Early in my tranport modelling career I went to Singapore to sales pitch MATSim. A hard sell but my audience was very polite and let me finish. It turned out that their only concerns were time and cost. Speciifcally, could we build it faster than a four-stage model? I asked <em>how much for a four-stage?</em> - 18 months aparently, and $,\($,\)$ squid.</p> <p>Despite this cost, such transport models are somewhat limited compared to language models. Despite taking years and vast sums of money to build, they only work for a single place and time. Increasingly they are also being pushed to model more complex scenarios in more precise ways, beyond the sensible boundaries of existing theory and practical implementation.</p> <p>So why not follow language modelling’s lead and throw data at the problem? The easy answer is that we don’t really have the data. Language models were trained on essentially the entire written output of humanity — the GPT-3 corpus alone was something like 45 terabytes (<a href="https://arxiv.org/abs/2005.14165">Brown et al., 2020</a>). The equivalent for travel behaviour would be a continuous, high-resolution record of where everyone goes, why, and with whom. We have travel diaries,and increasingly, some mobile phone data. But it is patchy, expensive, and often flawed or insufficient.</p> <p>So transport modellers occupy an awkward middle ground. Not enough data to go fully empirical. Too much complexity to stay fully theoretical.</p> <h1 id="an-opportunity">An opportunity</h1> <p>Thanks to an apparent disregard for intellectual property, trademarks, and even personal privacy - perhaps irresponsibility - language models represent the outer limits of what is possible. But more efficient, safe, and sensible data-driven approaches certainly exist (see my previous love letter to <a href="https://fredshone.github.io/blog/2025/VAEs/">VAEs</a>).</p> <p>LLMs have also shown us the value of scale. They generalise to new situations using context, without retraining, so don’t need to be constantly retrained. They achieve this in part through <a href="https://en.wikipedia.org/wiki/Transfer_learning">transfer learning</a> — shifting knowledge learned in one domain to another.</p> <p>Perhaps we can then imagine a transport model that works anywhere, at any time, and for any scenario required. We could have a model that predicts human choices, whether in a rural town in the US or a European city, in the near or far future, for scenarios ranging across domains and applications.</p> <h1 id="the-fundamental-problem">The fundamental problem</h1> <p><strong><em>Should a transport model hallucinate?</em></strong></p> <p>Modern LLMs have arguably already moved beyond language and increasingly imitate human behaviours. By training on our data they have learnt to imitate our ethics, our choices, and even our beliefs. But they also can’t be trusted to (i) be correct, and (ii) more subtly, to represent reality faithfully.</p> <p>I’m going to argue that being incorrect — or hallucinating, if you prefer — is not such a big deal. Simply put, transport models are wrong anyway. Predicting the future is just too hard. Not that I think we can’t do better.</p> <p>The latter problem, however — representing the diverse and often unexpected range of human decision-making — matters. LLMs are increasingly being shown to <a href="https://arxiv.org/html/2504.05228v1">homogenise</a> towards the most common outputs - mode collapse on a grand scale, perhaps. Not everyone should buy a car, get an office job, and work nine to five in our models. Someone has to buy a bike, open a bookshop, and enjoy early morning walks. Someone also has to work two night shifts, pick up the kids at mid-day because they have a fever, and unwisely buy a motorbike. Diversity is important - because it’s real.</p> <h1 id="the-rl-problem">The RL problem</h1> <p><strong><em>Reinforcement learning broke language models open. It will probably not be so useful for us.</em></strong></p> <p>The leap from “impressive autocomplete” to something disturbingly competent is substantially the product of <a href="https://arxiv.org/abs/2203.02155">reinforcement learning from human feedback</a> (Ouyang et al., 2022): train the model to produce outputs humans prefer, penalise the bad ones, let it optimise. This works because there exists a coherent notion of a <em>good</em> answer.</p> <p>Now try to apply this to human behaviour modelling. We have utility theory to maximise — this gets us about as far as predicting whether a person will take the bus. But what about when to get groceries? Pick up a coffee? See a friend? As we push models further, there is no clean objective function.</p> <p>Even when we can specify one, like <em>get to work on-time!</em>, we would not want to optimise for it. The whole point is to predict what real, flawed, inconsistent humans will actually do — not what perfectly rational agents would do. We want the person who drives to the gym and takes the lift. We want the commuter who takes the slow train on a sunny day. Super-human performance is the goal for language models. For human behaviour models, it would be a catastrophic failure mode.</p>]]></content><author><name></name></author><category term="Dev"/><category term="ML,"/><category term="LLM"/><summary type="html"><![CDATA[parallels between modelling language and modelling people]]></summary></entry><entry><title type="html">A soft introduction to VAEs</title><link href="https://fredshone.github.io/blog/2025/VAEs/" rel="alternate" type="text/html" title="A soft introduction to VAEs"/><published>2025-09-26T00:00:00+00:00</published><updated>2025-09-26T00:00:00+00:00</updated><id>https://fredshone.github.io/blog/2025/VAEs</id><content type="html" xml:base="https://fredshone.github.io/blog/2025/VAEs/"><![CDATA[<p>Variational Auto-encoders (VAEs) by Kingma and Welling are generative models from back when in 2014 (<a href="https://arxiv.org/pdf/1312.6114">paper</a>).</p> <p>In the ML domain that’s ancient history. But VAEs are still useful and arguably underpinning many of the latest generative modelling achievements.</p> <p>This post will cover:</p> <ul> <li>A light primer to VAEs, no maffs</li> <li>Good applications</li> <li>VAEs today</li> <li>Lowlights</li> </ul> <p>Hopefully on the way we will elicit some new understanding and appreciations, or even love, of VAEs.</p> <h1 id="primer">Primer</h1> <p>When you first google/prompt/ask about VAEs you will randomly be assigned to one of two ways, the <em>easy</em> way or the <strong>hard</strong> way.</p> <p>The <strong>hard</strong> way, or textbook way, starts with some patter about variational Bayesian methods, progresses to a definition of the Evidence Lower Bound (ELBO), before finally <a href="https://yugeten.github.io/posts/2020/06/elbo/">degenerating</a> into alternative decompositions of the resulting loss function. This is a difficult path that is best trodden later.</p> <h2 id="the-auto-encoder">The auto-encoder</h2> <p>The <strong>easy</strong> way starts with a description of the lesser Auto-encoder (AE), a simple design that compresses data into a latent bottleneck. Data is passed into the model, squeezing it down via an encoder into a compressed latent vector (the bottleneck), then un-squeezing via a decoder back to the original data. The encoder and decoder can be somewhat arbitrary neural nets.</p> <p>The AE is then trained for this compression and de-compression task by minimising the difference between the data input and output. This difference is called the <em>reconstruction loss</em>. The closer the better. If you’re thinking this sounds like some kind of compression algorithm, then yes, that’s fine.</p> <p>Consider a dataset of images of dogs. During training, each image is passed into the encoder, compressed down to a smaller-dimensional vector, then un-compressed back to the original image (approximately) via the decoder. Once trained the encoder provides a mapping from images into the latent representation, typically called \(Z\), and the decoder provides a mapping from \(Z\) back to the (approximate) original image.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  ┌─────────────┐                              ┌─────────────┐    
  │  \          │ ─                          ─ │             │    
  │   (. .)\    │  ──       ┌────-─┐       ──  │  /(. .)\  / │    
  │    (*)____  │    ───    |  z0  |    ───    │    (o)___/  │    
  │    /     |\ │       ──  |  z1  | ──        │   /     |   │    
  │   / |--\ | \│-Encoder─&gt; |  z2  | -Decoder─&gt;│  / |--\ |   │    
  │ (_)(_) (_)  │       ──  |  ..  | ──        │ (_)(_) (_)  │    
  │             │    ───    |  zn  |    ───    │             │    
  │             │  ──       └─────-┘       ──  │             │    
  │             │ ─                          ─ │             │    
  └─────────────┘                              └─────────────┘    
                                                                    
</code></pre></div></div> <p>By squeezing images, which have many dimensions, through compressed latent dimensions, we force the AE to try to make efficient use of this latent. A consequence of this is that similar images are encoded close to each other in the latent vector. Consider an MNIST example, i.e. passing images of handwritten digits (zero to nine) through a 2D vector \((z_0, z_1)\). The digits might end up distributed across the latent space something like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>z1| 
  |    00             111
  |   000             1111
  |  0000     6666     111
  |           6666    
  |      99                444
  |    99999               4444 
  |     9       88    33      44
  |           88888   333 
  |             88   333     77
  |     555                 77
  |     555     22222       77
  |              222
  |________________________________
                                z0
</code></pre></div></div> <p>Similar digits are clustered together. For example the ones are close to other ones. But also maybe sevens that look a bit like fours are close to fours that look like sevens and so on.</p> <p>This general idea of finding and using compressed representations of data is pretty fundamental to both (i) the core usefulness of VAEs, and (ii) their use in state of the art generative approaches used today. So keep in mind.</p> <p>Our AE is pretty cool but it’s not a very good generative model. It’s more like an elaborate way of clustering or compressing. We could take some random samples from the latent space and decode them into images, and some of them might look realistic, but most not. The best we could probably manage is to interpolate between some of the training data latent values, ideally two that are close together, in which case hopefully the decoder will spit out something similar.</p> <p>For example we could sample between where we know the 6s and 8s are and get a digit that looks kind of like a 6 and kind of like an 8:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>z1| 
  |    00             111
  |   000             1111
  |  0000     6666     111
  |           6666    
  |      99     |          444
  |    99999    X          4444 
  |     9       |     33      44
  |           88888   333 
  |             88   333     77
  |     555                 77
  |     555     22222       77
  |              222
  |________________________________
                                z0
</code></pre></div></div> <p>Similarly, back to our doggy AE, we could sample between an image of a poodle and a labrador to get a labradoodle:</p> <pre><code class="language-┌─────────────┐"> z1                 ───  |     |  _    _  _  | 
 |               ───     |     | ( )  ( )( ) | 
 |  P          ───       |     | | \__| | /  |
 |   \      ──-Decoder-&gt; |     |  __    |    |
 |    X        ───       |     | |/   |(*)|  | 
 |     \         ───     |     | \   /(. .)\ | 
 |      L           ───  |     |             |
 └──────────z0                 └─────────────┘
</code></pre> <p>But more likely we’d get nonsense because the model hasn’t been trained to use the latent space this way. This general idea of using the whole latent space and maybe even making it meaningful — more up could be a happier dog, more right could be a smaller dog for example — is good intuition and a nice segue into VAEs.</p> <p>More formally we can think of the decoder as providing some conditional probability distribution \(p(x \mid z)\), where \(p(x)\) is our target distribution, and \(p(z)\) the latent distribution. The reason why the decoder is not providing a good generative model is because \(p(z)\) isn’t defined, and worse, is probably a nightmare landscape of sparse and discontinuous probability densities. So that if we try to sample from it, we usually get nonsense from the decoder and certainly we are unable to recover the target distribution \(p(x)\).</p> <h2 id="the-variational-auto-encoder">The Variational auto-encoder</h2> <p>The VAE imposes a prior distribution on the latent space. Traditionally a (multivariate) normal distribution, \(p(z) \sim \mathcal{N}(0, I)\). So that the latent space becomes denser, smoother and more usefully sampled from.</p> <p>This prior is imposed during training by adding an additional component to the loss function that encourages the latent to be normally distributed. So now our loss function has two parts; (i) the reconstruction loss, and (ii) a regularisation loss that tries to keep the latent distribution normally distributed. This regularisation is typically Kullback–Leibler divergence (\(D_{KL}\)).</p> \[\mathcal{L} = \mathcal{L}_{\text{reconstruction}} + D_{KL}\] <p>If training goes well the decoder learns \(p(x \mid z)\), i.e. our target distribution \(p(x)\) conditional on the latent \(z\), but now with \(z\) nicely distributed as a normal distribution. So we now sample new \(z\) values from \(\mathcal{N}(0, I)\), pass them into the decoder, and out comes a new sample from \(p(x)\).</p> <h1 id="applications">Applications</h1> <h2 id="density-representation">Density representation</h2> <p><strong><em>VAEs are good at density estimation</em></strong></p> <p>But what is density estimation versus sample quality and why does it matter?</p> <p>Well you should really read about evaluating generative models in my previous post. But in a nutshell, generative models tend not to be able to have their cake and also eat it. They can be very good at making realistic images (high sample quality) but not at generating the full distribution of all possible images (density estimation), for example. In the case of image generation, this is no problem. Sample quality dominates - <em>does this image of a person have the correct number of fingers?</em></p> <p>But for my applications, where I want to represent realistic distributions of populations of people, density estimation is key.</p> <p>VAEs explicitly consider the likelihood of all training samples as being distributed across the latent space. By keeping the latent space smooth and dense, a VAE is forced to encode the full breadth of data, not just the easy parts. This makes VAEs well suited to applications where diversity matters — where you need your model to represent the full distribution, not just its greatest hits.</p> <h2 id="efficiency">Efficiency</h2> <p><strong><em>VAEs are great (the best I think) for when you don’t have much data or compute</em></strong></p> <p>The smooth latent space also makes VAEs very data efficient compared to alternatives. Because the encoder and decoder share a structured, continuous latent, the model generalises well from limited data. Where a GAN or auto-regressive approach (think LLM) might need to see millions of examples to learn a good distribution, the VAE’s regularisation provides a strong inductive bias that does some of that work for free.</p> <p>We can abstractly think of a VAE as spreading its training data as points across a normal distribution. We can then sample infinitely from the gaps between the training data and approximately maintain the original distribution of data. It might be helpful to think of a VAE as learning to interpolate sensibly between data.</p> <p>This simplification comes at some cost. Firstly, we need to be careful during training to actually make sure the data is correctly distributed across the latent. Secondly, those spaces between the data are not necessarily well mapped by the decoder back to nice (high quality) samples. So the outputs from VAEs are typically considered as low quality. In the case of image generation, they are a bit fuzzy, for example.</p> <h1 id="vaes-today">VAEs today</h1> <p>The VAE is arguably more relevant now than it was in 2014, just not always under its own name.</p> <p>VAE-style encoders appear in many models, when an efficient and structured latent representation is useful. The latent diffusion model <a href="https://arxiv.org/abs/2112.10752">(Rombach et al., 2022)</a> — the architecture behind Stable Diffusion and many of its descendants — uses a VAE to compress images into a compact latent space before the diffusion process operates on them. The diffusion model never touches pixels directly; it works in the compressed latent space from the VAE. This is computationally far cheaper and, empirically, produces better results. The VAE here is not the generative model, but it is load-bearing infrastructure.</p> <p>The VAE is perhaps best understood not as a generative model in its own right, but as a very good way of building latent spaces. That turns out to be useful almost everywhere.</p> <h1 id="the-nasty-bits-of-vaes">The nasty bits of VAEs</h1> <h2 id="tuning-a-vae">Tuning a VAE</h2> <p>VAEs require a balance between the reconstruction and regularisation (\(D_{KL}\)) losses, controlled by a hyperparameter \(\beta\) formalised as the \(\beta\)-VAE by <a href="https://openreview.net/forum?id=Sy2fchgb">Higgins et al. (2017)</a>. High \(\beta\) prioritises regularisation, pushing the latent hard towards the prior (smooth and dense and correctly distributed), but at the cost of reconstruction quality. Low \(\beta\) prioritises reconstructions but produces a messier latent that is harder to sample from.</p> <p>This forms a trade-off between two failure mechanisms; (i) poor reconstruction by the decoder, and (ii) poor regularisation of the latent. Finding a good trade-off typically requires plenty of search and evaluating generated outputs.</p> <p>It’s worth noting that \(D_{KL}\) is computed per sample, i.e. it is a local measure of how well each individual encoding matches the prior. A model can have a low average \(D_{KL}\) while still having pockets of the latent that are poorly regularised and will produce nonsense when sampled. The Wasserstein Auto-encoder (WAE) <a href="https://arxiv.org/abs/1711.01558">(Tolstikhin et al., 2018)</a> addresses this by replacing the per-sample \(D_{KL}\) with a global distributional penalty, which is more directly aligned with the actual generative objective.</p> <h2 id="conditionality-is-hard">Conditionality is hard</h2> <p>Conditional VAEs <a href="https://proceedings.neurips.cc/paper/2015/hash/8d55a249e6baa5c06772297520da2051-Abstract.html">(Sohn et al., 2015)</a> — where you want to generate some output conditional on some input (in addition to the random sample of \(z\)) — are tricky. Structurally they are straightforward. Pass \(y\) into both encoder and decoder, and the model learns to generate \(x\) given \(y\) in addition to getting random variation from \(z\).</p> <p>But in practice, VAEs have a habit of ignoring \(y\), especially if \(y\) is weak or confused. This is the <em>posterior collapse</em> problem, and it is a specific case of a more general issue: the VAE will always find the laziest path to minimising its loss, and that path often involves not using information you wanted it to use.</p> <p>Various fixes exist — \(D_{KL}\) annealing, free bits, weakening the decoder — but they are all patches on a fundamental tension. The reconstruction loss wants to use every available signal. The \(D_{KL}\) wants to forget everything that isn’t in the prior. Threading between these two pressures is the core difficulty of training VAEs, and conditionality makes it harder.</p>]]></content><author><name></name></author><category term="Dev"/><category term="ML,"/><category term="VAEs"/><summary type="html"><![CDATA[some VAE intricacies]]></summary></entry><entry><title type="html">Evaluation</title><link href="https://fredshone.github.io/blog/2025/evaluation/" rel="alternate" type="text/html" title="Evaluation"/><published>2025-07-13T00:00:00+00:00</published><updated>2025-07-13T00:00:00+00:00</updated><id>https://fredshone.github.io/blog/2025/evaluation</id><content type="html" xml:base="https://fredshone.github.io/blog/2025/evaluation/"><![CDATA[<p>Two years into PhD. How to measure the quiet trickle of progress?</p> <p>I’ve been carefully writing and rewriting a paper. Sometimes in a furious state. But now, more or less past the finish line, I have the chance to look back and write some of the stuff here that got lost in the process.</p> <p>My research is just old ideas. But it’s new ideas for an old problem, and as such, how to play the game is slightly in flux. The following will be about how to play the game of measuring success, i.e. of <em>evaluation</em>.</p> <h1 id="primer">Primer</h1> <p>The mention of <em>evaluation</em> can be followed by an immediate mental slide into some metric or other, a distance, a probability, or some other quantity. But what we measure matters more than how we measure it.</p> <p>We evaluate our model/device/thing to convince ourselves, plus some reviewers and all other interested parties, that our thing is good. Specifically, that it is good, or fit, for some specific purpose or application. Maybe it predicts the future. Maybe it can tell a dog from a cat. Or maybe it’s for zesting lemons. The important point, obviously but easily forgotten, is that our evaluation needs to demonstrate fitness for that purpose. It should simulate the real intended use of our model/device/thing as closely as possible.</p> <h1 id="evaluation-in-machine-learning">Evaluation in machine learning</h1> <p><strong><em>For the ML congregation, the train, validate, test divide is sacrosanct, and benchmarking tasks are plentiful.</em></strong></p> <p>Some research whittles away at evaluation benchmarks so entrenched and emblematic of progress that tiny fractions of progress grant canonisation. These highly reproducible evaluation frameworks are at least partially responsible for the rapid ascendancy of ML.</p> <p>There are, however, some wrinkles. With attention and careers on the line, there is the temptation to misbehave, and even without any bad apples, test data manages to creep into train or validation sets. Big models have also paved the way for progress by grad-descent, where state-of-the-art model designs, specifications, and seeds are achieved using a faithful flock of grad students.</p> <p>So you follow the lore, you split your data and evaluate. But still, people look over your shoulder and grumble. The split is supposed to simulate the real application; random splits may not suffice. If you predict the future, then testing on random slices from the past is unrealistic. If your model is for a new place, at a new time, or in new conditions, your test data should simulate as such.</p> <p>Most recently, model capabilities have ascended past human experts and saturated the classic benchmarks. We now train models to undertake more complex and difficult tasks. It’s increasingly not so obvious or easy to evaluate such requirements. Some complex tasks, like mathematical proofs or coding, are intrinsically verifiable, but others are not. How to evaluate whether customers are happy with your bot or if your Ghibli-style self-portrait is any good?</p> <h2 id="evaluation-elsewhere">Evaluation elsewhere</h2> <p><strong><em>Before ML, there was the Age of Statistics.</em></strong></p> <p>The statisticians of the time, without much data or compute, built models upon structures of knowns, in which the capacity to learn was carefully rationed and controlled. Parameters were few, unknown ones especially, and so capacity was limited and tasks simplified. Without much data to go around, nothing was withheld and the evaluation of a model was more an evaluation of the modeller, about what they knew or could do, rather than what the model could do. But this wasn’t a problem, because back then you could look at a model, see it all, and more or less understand it and know that it was good.</p> <p>Having a little model that you can truly understand is reasonably sufficient grounds for good evaluation. But little models can’t do difficult tasks, and so we sometimes stack them up. Human behaviour is a nice example. Pick a simple choice - <em>should I travel to work by car or by bike?</em> - find some data, specify a little model, train it, maybe test it. Looks fine. Add some complexity - <em>should I also go shopping today?</em> No problem. Stick another model on. But then, <em>what time should I leave the house? Isn’t tonight bowling night?</em> and so on. Quickly, we have a very complicated collection of simple models, which we don’t truly understand, and we’re not sure how well we truly evaluated it/them.</p> <h2 id="generative-modelling">Generative modelling</h2> <p><strong><em>Enter generative AI, the amalgamation of probabilistic modelling and deep machine learning.</em></strong></p> <p>A potent combination of uncertainty and opacity. In its purest case, a generative model aims to learn the distribution of some observed data sample. This data and distribution might be images of cats, or 18th-century poetry, or for me, people and their choices. But we’ll just call the distributions \(P(X)\).</p> <p>Once you’ve learnt \(P(X)\), you can generate new samples from it. As well as access to a near-infinite diversity of cat images, generating new samples allows us to surrogate processes that are either too complex or unknown to be otherwise simulated. Like a chat with a human.</p> <p>Generative modellers are also working with big, complex distributions. The possible space of all possible cat images, for example, is massive. Consider an image size of N pixels, each pixel has three channels (RGB) with 256 possible values. This forms a joint distribution of pixel channel values, with \(256^{3N}\) possible images.</p> <h2 id="generative-evaluation">Generative evaluation</h2> <p>When we tackle a purely generative problem, we have <em>some</em> data from \(X\) and use this to model \(P(X)\). Skipping forward a bit, we’ve finished training our model and we want to evaluate it. But where is our test split? Can we even make one? Consider the beloved (my words) Variation Auto-Encoder (VAE) that (after some fiddling) provides a mapping between a random latent prior \(P(Z) \sim N(0,I)\) and our desired distribution \(P(X)\), such that when we want a new cat image, poem or person, we randomly sample a \(z\), pass it through the model, and out pops a brand new cat image, poem or person. We can test some mechanisms of the model, but ultimately, the evaluated case needs to capture this generative process, which is always out of scope of the training data, because its input is a random sample from \(Z\).</p> <hr/> <p><strong>NOTE</strong></p> <p>Most generative models actually use some form of conditionality. The big boys, image and text generation models, are typically text prompted, for example. But usually there is at least some generative or probabilistic process remaining. For example, they learn \(P(X \mid Y)\).</p> <hr/> <p><strong><em>New recipe then. Density estimation.</em></strong></p> <p>Step one. We claim we have a dataset worthy of all real cat images. Nobody will say otherwise; the data is fine. Step two. We measure out a large number of generated cat images from our model. Step three. Mash the real cat images into a probability distribution. Step four. Mash the synthetic images into a probability distribution. Final step. Do they look similar? Yes? Great! You have made a delicious generative model. No? Relax! Try re-mashing harder.</p> <p>The sticking point is that estimating probability distributions from a finite number of samples, sprinkled across a very large, complex and unknown distribution, is somewhere between really difficult, infeasible and impossible. Upon minor reflection, this was in fact the whole point of generative modelling. We want to fill in those gaps in the data, because we don’t know them. But because we don’t know them, we are now going to struggle to evaluate the quality of our filling.</p> <p>We could withhold a sprinkling of real data as a test set, but it won’t be useful in the great expanse of possible \(\mid X \mid\) cat images.</p> <h2 id="quality-vs-diversity">Quality vs diversity</h2> <p>Nobody is perfect. But if we were perfect, at density estimation, then we would expect our generated samples to be both realistic individually and in aggregate. For example, we would expect our cat images to individually look like cats, and collectively, to span the full diversity of possible cats.</p> <p>But we are not perfect and wouldn’t even know if we were because we don’t know what perfect looks like. So, as we fail, it is useful to know how we failed. Did we drift away from individual quality or from collective diversity? A common indicator of poor image quality was blurryness, and then as things got better/bigger, too many fingers. Over the same time period, people became more chill about diversity. Yes, we would like some diversity. We certainly don’t want to collapse into no diversity. But do we need to see everything?</p> <p>It is probably easier to evaluate individual quality in more cases. You can just have another model count fingers or even pay a human to give the thumbs up or down. Plus, individual quality is more of a priority in many applications. Why train a bot to speak the truth when you can just have it be agreeable? Just please don’t be Mecha Hitler. As such, there has been a shift towards evaluating quality over diversity.</p> <h1 id="final-thoughts">Final thoughts</h1> <p>But generative diversity really matters sometimes. My work generates human choices, specifically the actions we take and when, over the course of a day. These vary both from person to person and for the same person, from day to day. Sometimes I go for drinks after work, sometimes I sit at home all day watching brain rot. To usefully and fairly model the real world, I want to model this diversity and I want to model it correctly.</p>]]></content><author><name></name></author><category term="Dev"/><category term="ML,"/><category term="evaluation"/><summary type="html"><![CDATA[measuring success]]></summary></entry><entry><title type="html">Overthinking a skinny XML parser with rust</title><link href="https://fredshone.github.io/blog/2023/skinny-xml-parsing-with-rust/" rel="alternate" type="text/html" title="Overthinking a skinny XML parser with rust"/><published>2023-07-20T14:14:00+00:00</published><updated>2023-07-20T14:14:00+00:00</updated><id>https://fredshone.github.io/blog/2023/skinny-xml-parsing-with-rust</id><content type="html" xml:base="https://fredshone.github.io/blog/2023/skinny-xml-parsing-with-rust/"><![CDATA[<p>Building a fast xml parser for the MATSim network format. Using Rust. Contents roughly as follows:</p> <ul> <li>Problem statement</li> <li>xml format</li> <li>Broad strategy</li> <li>Initial implementation</li> <li>Benchmark</li> <li>Fiddling</li> </ul> <h1 id="problem-statement">Problem statement</h1> <p>I have a big project in mind. For which parsing and holding some big data structures in memory is only a tiny part. But a nice enough place to start.</p> <p>I am going to read a MATSim <em>network</em> file (an xml) and store it (but just the bits i need) in memory. in this case <em>network</em> is referring to transport <em>network</em>, ie roads, rail etc.</p> <p>networks are usefully represented as (directed) graphs, where junctions are vertices/nodes and the links between them edges. But for starters, I’m just going to focus on reading the link lengths only.</p> <p>I want to get these lengths into memory fast. But in the grandiose scheme of things, I want to access access and potentially duplicate the data quickly. So it’s going to be more useful to have (i) a really fast to access data structure, and (ii) minimal memory impact.</p> <hr/> <p><strong>NOTE</strong></p> <p><em>Fast data structure?</em> - I mean that, given a link id, I want to be able to get the length of that link as quickly as possible.</p> <hr/> <h1 id="matsim-xml-format">MATSim XML Format</h1> <p><a href="https://www.matsim.org/">MATSim</a> (Multi Agent Transport Simulation) is a big open source project. The full scope of which is beyond this post. So we just focus in on the MATSim network file format:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;network&gt;</span>

	<span class="nt">&lt;nodes&gt;</span>
		<span class="nt">&lt;node</span> <span class="na">id=</span><span class="s">"nodeA"</span> <span class="na">x=</span><span class="s">"0.0"</span> <span class="na">y=</span><span class="s">"0.0"</span> <span class="nt">&gt;&lt;/node&gt;</span>
		<span class="nt">&lt;node</span> <span class="na">id=</span><span class="s">"nodeB"</span> <span class="na">x=</span><span class="s">"100.0"</span> <span class="na">y=</span><span class="s">"0.0"</span> <span class="nt">&gt;&lt;/node&gt;</span>
	  <span class="c">&lt;!-- and so on... --&gt;</span>
	<span class="nt">&lt;/nodes&gt;</span>

	<span class="nt">&lt;links&gt;</span>
		<span class="nt">&lt;link</span> <span class="na">id=</span><span class="s">"linkE"</span> <span class="na">from=</span><span class="s">"nodeA"</span> <span class="na">to=</span><span class="s">"nodeB"</span> <span class="na">length=</span><span class="s">"100.0"</span> <span class="na">modes=</span><span class="s">"bus,car"</span> <span class="nt">&gt;&lt;/link&gt;</span>
		<span class="nt">&lt;link</span> <span class="na">id=</span><span class="s">"linkW"</span> <span class="na">from=</span><span class="s">"nodeB"</span> <span class="na">to=</span><span class="s">"nodeA"</span> <span class="na">length=</span><span class="s">"102.0"</span> <span class="na">modes=</span><span class="s">"bus,car"</span> <span class="nt">&gt;&lt;/link&gt;</span>
	  <span class="c">&lt;!-- and so on... --&gt;</span>

<span class="nt">&lt;/network&gt;</span>

</code></pre></div></div> <p>Two things to note at this point; (i) nodes (vertices if you’re graph thinking) are defined first, then (ii) links are defined second and contain the desired length attribute.</p> <p>To get us started we’re going to parse into the following structure:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">LinkLengths</span> <span class="o">=</span> <span class="n">HashMap</span><span class="o">&lt;</span><span class="nb">String</span><span class="p">,</span> <span class="nb">f32</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div> <p>Each link will be identified as by it’s id value (as a String) and the length as an f32 (the Rust primitive 32-bit floating point type).</p> <p>We are going to keep this in a <code class="language-plaintext highlighter-rouge">Links</code> struct so that we can tack on methods and extend in future:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">struct</span> <span class="n">Links</span> <span class="p">{</span>
    <span class="n">lengths</span><span class="p">:</span> <span class="n">LinkLengths</span>
<span class="p">}</span>
</code></pre></div></div> <hr/> <p><strong>NOTE</strong></p> <p>f32 is dumb. Firstly we probably don’t want negative numbers, they do not make sense for a road network. Secondly f32 is unnecessarily large. So we might revisit this later.</p> <hr/> <h1 id="broad-strategy">Broad Strategy</h1> <p>Firstly I’m just going to get it running. The excellent <a href="https://github.com/tafia/quick-xml">quick_xml</a> is going to spin through the xml and we’re going to populate our <code class="language-plaintext highlighter-rouge">LinkLengths</code>.</p> <p>Then we’re going to test and establish some benchmarks for:</p> <ul> <li>parsing the xml (fast is good but this isn’t really a top priority)</li> <li>read time (we’re going to ask for the length of a bunch of links)</li> </ul> <p>Then we’re going to try out a few obvious improvements:</p> <ul> <li>stress about using String and or clone</li> <li>squeeze the data types</li> <li>completely reimagine how we are going to be able to use the data (ie reindex and use an array)</li> </ul> <h1 id="initial-implementation">Initial Implementation</h1> <h2 id="reader">Reader</h2> <p>First I set up a helper funcion to set up the <a href="https://docs.rs/quick-xml/latest/quick_xml/index.html">quick_xml reader</a>. I engage in some <code class="language-plaintext highlighter-rouge">Box</code> shenanigans because the full version also accepts a decoding BufReader for zipped files.</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">type</span> <span class="n">XMLReader</span> <span class="o">=</span> <span class="nn">quick_xml</span><span class="p">::</span><span class="n">Reader</span><span class="o">&lt;</span><span class="nb">Box</span><span class="o">&lt;</span><span class="k">dyn</span> <span class="n">BufRead</span><span class="o">&gt;&gt;</span><span class="p">;</span>

<span class="k">pub</span> <span class="k">fn</span> <span class="nf">from_path</span><span class="p">(</span><span class="n">path</span><span class="p">:</span> <span class="k">impl</span> <span class="nb">AsRef</span><span class="o">&lt;</span><span class="n">Path</span><span class="o">&gt;</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="n">XMLReader</span><span class="p">,</span> <span class="n">XMLReaderError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">open</span><span class="p">(</span><span class="o">&amp;</span><span class="n">path</span><span class="p">)</span><span class="nf">.context</span><span class="p">(</span><span class="s">"Could not open file"</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">reader</span> <span class="o">=</span> <span class="nn">Box</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="nn">BufReader</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">file</span><span class="p">));</span>
    <span class="k">let</span> <span class="n">xml_reader</span> <span class="o">=</span> <span class="nn">quick_xml</span><span class="p">::</span><span class="nn">Reader</span><span class="p">::</span><span class="nf">from_reader</span><span class="p">(</span><span class="n">reader</span><span class="p">)</span>
    <span class="n">xml_reader</span><span class="nf">.check_comments</span><span class="p">(</span><span class="k">false</span><span class="p">)</span><span class="nf">.check_end_names</span><span class="p">(</span><span class="k">false</span><span class="p">);</span>
    <span class="nf">Ok</span><span class="p">(</span><span class="n">xml_reader</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div> <hr/> <p><strong>NOTE</strong></p> <p>The quick_xml docs say we can disable checking in various places and go faster. This is only sensible if we trust the ingested xml, which I choose to do for now.</p> <hr/> <p><strong>NOTE</strong></p> <p>Also note that I’ve been careful/pedantic/naive and created some custom errors using <code class="language-plaintext highlighter-rouge">thiserror</code>. I’m not including any explanation of these. If you’re interested in exact the final implementation then LINK INCOMING.</p> <hr/> <h2 id="parsing-xml">Parsing XML</h2> <p>The <code class="language-plaintext highlighter-rouge">XMLReader</code> we have created is just a fancy quick_xml <code class="language-plaintext highlighter-rouge">Reader</code> struct. This is used to loop through the xml contents returning them in order as different quick_xml::events::Event enum variations. We can then run a match statement on these events and do something with them depending on thier contents. The general pattern is:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="k">mut</span> <span class="n">reader</span> <span class="o">=</span> <span class="nf">from_path</span><span class="p">(</span><span class="o">&amp;</span><span class="n">path</span><span class="p">)</span>
<span class="k">let</span> <span class="k">mut</span> <span class="n">buffer</span> <span class="o">=</span> <span class="nn">Vec</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
<span class="k">loop</span> <span class="p">{</span>
  <span class="k">match</span> <span class="n">reader</span><span class="nf">.read_event_into</span><span class="p">(</span><span class="o">&amp;</span> <span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="o">?</span> <span class="p">{</span>
    <span class="nn">Event</span><span class="p">::</span><span class="nf">Start</span><span class="p">(</span><span class="n">event</span><span class="p">)</span> <span class="k">=&gt;</span> <span class="nf">do_something</span><span class="p">(</span><span class="n">event</span><span class="p">),</span>
    <span class="nn">Event</span><span class="p">::</span><span class="n">Eof</span> <span class="k">=&gt;</span> <span class="k">break</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="p">{},</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>There are various different Events that make up what I generally think of as xml elements. We are going to ignore everything apart from <code class="language-plaintext highlighter-rouge">Event::Start</code> which will hold the information we need for our link lengths map (eg <code class="language-plaintext highlighter-rouge">&lt;link id="linkE" from="nodeA" to="nodeB" length="100.0" modes="bus,car" &gt;</code>), and <code class="language-plaintext highlighter-rouge">Event::Eof</code> which we use to identify the end of the file so we can break the loop.</p> <hr/> <p><strong>NOTE</strong></p> <p>quick_xml is reading our data into the <code class="language-plaintext highlighter-rouge">buffer</code> and generally doing as best as it can to avoid allocating memory. The opportunity for us is to then do the absolute minimum with this buffer data, only using what we need.</p> <hr/> <p><code class="language-plaintext highlighter-rouge">Event::Start</code> holds a <code class="language-plaintext highlighter-rouge">StartEvent</code> struct which has a <code class="language-plaintext highlighter-rouge">name</code> field. This is essentially just a view into the buffer, but we can use it to look for elements named “link” and then do something with it:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">loop</span> <span class="p">{</span>
  <span class="k">match</span> <span class="n">reader</span><span class="nf">.read_event_into</span><span class="p">(</span><span class="o">&amp;</span> <span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="o">?</span> <span class="p">{</span>
    <span class="nn">Event</span><span class="p">::</span><span class="nf">Start</span><span class="p">(</span><span class="n">event</span><span class="p">)</span> <span class="k">if</span> <span class="n">event</span><span class="nf">.name</span><span class="p">()</span><span class="nf">.as_ref</span><span class="p">()</span> <span class="o">==</span> <span class="s">b"link"</span> <span class="k">=&gt;</span> <span class="p">{</span>
      <span class="nf">do_something</span><span class="p">(</span><span class="n">event</span><span class="p">)</span>
    <span class="p">},</span>
    <span class="nn">Event</span><span class="p">::</span><span class="n">Eof</span> <span class="k">=&gt;</span> <span class="k">break</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="p">{},</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="parsing-xml-attributes">Parsing XML Attributes</h2> <p>So now <code class="language-plaintext highlighter-rouge">do_something</code> is only going to be called on link elements. These elements contain the xml attributes, why quick_xml exposes as <code class="language-plaintext highlighter-rouge">Attributes</code> via <code class="language-plaintext highlighter-rouge">event.attributes()</code>:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">loop</span> <span class="p">{</span>
  <span class="k">match</span> <span class="n">reader</span><span class="nf">.read_event_into</span><span class="p">(</span><span class="o">&amp;</span> <span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="o">?</span> <span class="p">{</span>
    <span class="nn">Event</span><span class="p">::</span><span class="nf">Start</span><span class="p">(</span><span class="n">event</span><span class="p">)</span> <span class="k">if</span> <span class="n">event</span><span class="nf">.name</span><span class="p">()</span><span class="nf">.as_ref</span><span class="p">()</span> <span class="o">==</span> <span class="s">b"link"</span> <span class="k">=&gt;</span> <span class="p">{</span>
      <span class="k">let</span> <span class="n">attributes</span> <span class="o">=</span> <span class="n">event</span><span class="nf">.attributes</span><span class="p">();</span>
      <span class="nf">do_something</span><span class="p">(</span><span class="n">attributes</span><span class="p">);</span>
    <span class="p">},</span>
    <span class="nn">Event</span><span class="p">::</span><span class="n">Eof</span> <span class="k">=&gt;</span> <span class="k">break</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="p">{},</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">Attributes</code> can be iterated though as individual <code class="language-plaintext highlighter-rouge">Attribute</code> structs. These contain a key and value field. We can now look at these keys, to identify the ones we need (<code class="language-plaintext highlighter-rouge">id</code> and <code class="language-plaintext highlighter-rouge">length</code>).</p> <hr/> <p><strong>NOTE</strong></p> <p>Reminder that <code class="language-plaintext highlighter-rouge">Attributes</code> and <code class="language-plaintext highlighter-rouge">Attribute</code> still contain fields that are slices of our buffer. quick_xml is “simply” making sure we look at the right bits.</p> <hr/> <p>I’m confident that <code class="language-plaintext highlighter-rouge">id</code> is always going to be the first attribute, so we can create a String called id as follows:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">loop</span> <span class="p">{</span>
  <span class="k">match</span> <span class="n">reader</span><span class="nf">.read_event_into</span><span class="p">(</span><span class="o">&amp;</span> <span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="o">?</span> <span class="p">{</span>
    <span class="nn">Event</span><span class="p">::</span><span class="nf">Start</span><span class="p">(</span><span class="n">event</span><span class="p">)</span> <span class="k">if</span> <span class="n">event</span><span class="nf">.name</span><span class="p">()</span><span class="nf">.as_ref</span><span class="p">()</span> <span class="o">==</span> <span class="s">b"link"</span> <span class="k">=&gt;</span> <span class="p">{</span>
      <span class="k">let</span> <span class="n">attributes</span> <span class="o">=</span> <span class="n">event</span><span class="nf">.attributes</span><span class="p">();</span>
      <span class="c1">// some wizardry to create an iterator</span>
      <span class="k">let</span> <span class="k">mut</span> <span class="n">iter_attributes</span> <span class="o">=</span> <span class="n">attributes</span><span class="nf">.with_checks</span><span class="p">(</span><span class="k">false</span><span class="p">)</span><span class="nf">.flatten</span><span class="p">();</span>

      <span class="c1">// get id (we "know" it will be first)</span>
      <span class="k">let</span> <span class="n">id_attribute</span> <span class="o">=</span> <span class="n">iter_attributes</span><span class="nf">.next</span><span class="p">()</span><span class="nf">.ok_or</span><span class="p">(</span><span class="nn">LinkParserError</span><span class="p">::</span><span class="nf">LinkIdReadFailure</span><span class="p">())</span><span class="o">?</span><span class="py">.value</span><span class="p">;</span>
      <span class="k">let</span> <span class="n">id</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">from_utf8</span><span class="p">(</span><span class="n">id_att</span><span class="nf">.to_owned</span><span class="p">())</span><span class="o">?</span><span class="p">;</span>

      <span class="nf">do_something</span><span class="p">(</span><span class="n">attributes</span><span class="p">);</span>
    <span class="p">},</span>
    <span class="nn">Event</span><span class="p">::</span><span class="n">Eof</span> <span class="k">=&gt;</span> <span class="k">break</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="p">{},</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">String::from_utf8</code> is creating a String struct from the buffer. Up until now we avoided any new allocations beyond the buffer.</p> <p>I’m less confident on the consistency of the location of the <code class="language-plaintext highlighter-rouge">length</code> attribute. If I was I would use something like <code class="language-plaintext highlighter-rouge">iter_attributes.take(3).next()</code>. Instead we are going to use <code class="language-plaintext highlighter-rouge">find_map</code>. This will take a function that should return <code class="language-plaintext highlighter-rouge">Some(f32)</code> in the event of a <code class="language-plaintext highlighter-rouge">length</code> attribute and otherwise <code class="language-plaintext highlighter-rouge">None</code>:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">parse_length</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="n">Attribute</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">f32</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="k">match</span> <span class="n">a</span><span class="py">.key</span><span class="nf">.into_inner</span><span class="p">()</span> <span class="p">{</span>
        <span class="s">b"length"</span> <span class="k">=&gt;</span> <span class="p">{</span>
            <span class="k">let</span> <span class="n">string</span> <span class="o">=</span> <span class="nn">str</span><span class="p">::</span><span class="nf">from_utf8</span><span class="p">(</span><span class="o">&amp;</span><span class="n">a</span><span class="py">.value</span><span class="p">)</span><span class="nf">.unwrap</span><span class="p">();</span>
            <span class="k">let</span> <span class="nb">float</span> <span class="o">=</span> <span class="n">string</span><span class="py">.parse</span><span class="p">::</span><span class="o">&lt;</span><span class="nb">f32</span><span class="o">&gt;</span><span class="p">()</span><span class="nf">.unwrap</span><span class="p">();</span>
            <span class="nf">Ok</span><span class="p">(</span><span class="nb">float</span><span class="p">)</span>
        <span class="p">},</span>
        <span class="n">_</span> <span class="k">=&gt;</span> <span class="nb">None</span>
    <span class="p">}</span> 
<span class="p">}</span>
</code></pre></div></div> <p>We use this in our loop to get the <code class="language-plaintext highlighter-rouge">length</code> value:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">loop</span> <span class="p">{</span>
  <span class="k">match</span> <span class="n">reader</span><span class="nf">.read_event_into</span><span class="p">(</span><span class="o">&amp;</span> <span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="o">?</span> <span class="p">{</span>
    <span class="nn">Event</span><span class="p">::</span><span class="nf">Start</span><span class="p">(</span><span class="n">event</span><span class="p">)</span> <span class="k">if</span> <span class="n">event</span><span class="nf">.name</span><span class="p">()</span><span class="nf">.as_ref</span><span class="p">()</span> <span class="o">==</span> <span class="s">b"link"</span> <span class="k">=&gt;</span> <span class="p">{</span>
      <span class="k">let</span> <span class="n">attributes</span> <span class="o">=</span> <span class="n">event</span><span class="nf">.attributes</span><span class="p">();</span>
      <span class="c1">// some wizardry to create an iterator</span>
      <span class="k">let</span> <span class="k">mut</span> <span class="n">iter_attributes</span> <span class="o">=</span> <span class="n">attributes</span><span class="nf">.with_checks</span><span class="p">(</span><span class="k">false</span><span class="p">)</span><span class="nf">.flatten</span><span class="p">();</span>

      <span class="c1">// get id (we "know" it will be first)</span>
      <span class="k">let</span> <span class="n">id_attribute</span> <span class="o">=</span> <span class="n">iter_attributes</span><span class="nf">.next</span><span class="p">()</span><span class="nf">.ok_or</span><span class="p">(</span><span class="nn">LinkParserError</span><span class="p">::</span><span class="nf">LinkIdReadFailure</span><span class="p">())</span><span class="o">?</span><span class="py">.value</span><span class="p">;</span>
      <span class="k">let</span> <span class="n">id</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">from_utf8</span><span class="p">(</span><span class="n">id_att</span><span class="p">)</span><span class="o">?</span><span class="p">;</span>

      <span class="c1">// get length (this could be anywhere later)</span>
      <span class="k">let</span> <span class="n">length</span> <span class="o">=</span> <span class="n">attributes</span><span class="nf">.find_map</span><span class="p">(</span> <span class="k">Self</span><span class="p">::</span><span class="n">parse_length</span> <span class="p">)</span>
      <span class="nf">.ok_or</span><span class="p">(</span><span class="nn">LinkParserError</span><span class="p">::</span><span class="nf">FindLengthError</span><span class="p">())</span><span class="o">??</span><span class="p">;</span>
    <span class="p">},</span>
    <span class="nn">Event</span><span class="p">::</span><span class="n">Eof</span> <span class="k">=&gt;</span> <span class="k">break</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="p">{},</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <hr/> <p><strong>NOTE</strong></p> <p>On handling errors: <code class="language-plaintext highlighter-rouge">parse_length()</code> uses <code class="language-plaintext highlighter-rouge">unwrap()</code> twice, panicking if either it can’t convert to <code class="language-plaintext highlighter-rouge">str</code> or parse that as a float.</p> <p>The fancier version propagates these errors up by returning a LinkParser error. This results in an awkward looking return type; <code class="language-plaintext highlighter-rouge">Option&lt;Result&lt;f32, LinkParserError&gt;&gt;</code> - ie an <code class="language-plaintext highlighter-rouge">Option</code> of a <code class="language-plaintext highlighter-rouge">Result</code>.</p> <p>In this case <code class="language-plaintext highlighter-rouge">None</code> means that the given <code class="language-plaintext highlighter-rouge">Attribute</code> was not a link length. No problem - we keep iterating.</p> <p><code class="language-plaintext highlighter-rouge">Some&lt;LinkParserError&gt;</code> means that it found the link length attribute key but failed to parse the value into a float.</p> <p><code class="language-plaintext highlighter-rouge">Some&lt;Ok&lt;f32&gt;&gt;</code> is what we are after but we then have to unpack all this.</p> <p>There is then the case where we only returned <code class="language-plaintext highlighter-rouge">None</code> - ie we failed to find the length attribute - in which case we <code class="language-plaintext highlighter-rouge">find_map</code> returns <code class="language-plaintext highlighter-rouge">None</code> and we raise a new error <code class="language-plaintext highlighter-rouge">FindLengthError</code>.</p> <p>This was certainly all overkill.</p> <hr/> <h2 id="finally">Finally</h2> <p>So now we have a way of getting <code class="language-plaintext highlighter-rouge">id</code> and <code class="language-plaintext highlighter-rouge">length</code> for each link we can put this logic in a constructor method for our <code class="language-plaintext highlighter-rouge">Links</code> struct. I simply add an empty hashmap (the <code class="language-plaintext highlighter-rouge">LinkLengths</code> type), populate this with <code class="language-plaintext highlighter-rouge">id</code> and <code class="language-plaintext highlighter-rouge">length</code>. Then return a populated <code class="language-plaintext highlighter-rouge">Links</code> struct:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">impl</span> <span class="n">Links</span> <span class="p">{</span>

    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">from_reader</span><span class="p">(</span><span class="k">mut</span> <span class="n">reader</span><span class="p">:</span> <span class="n">XMLReader</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="k">Self</span><span class="p">,</span> <span class="n">LinkParserError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">let</span> <span class="k">mut</span> <span class="n">lengths</span> <span class="o">=</span> <span class="nn">LinkLengths</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
        <span class="k">let</span> <span class="k">mut</span> <span class="n">buf</span> <span class="o">=</span> <span class="nn">Vec</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
        <span class="k">loop</span> <span class="p">{</span>
            <span class="c1">// omited logic used to get id and length</span>
            <span class="c1">// add to hashmap</span>
            <span class="n">lengths</span><span class="nf">.insert</span><span class="p">(</span><span class="n">id</span><span class="p">,</span> <span class="n">length</span><span class="p">);</span>
        <span class="p">}</span>
        <span class="nf">Ok</span><span class="p">(</span><span class="n">Links</span> <span class="p">{</span>
            <span class="n">lengths</span>
        <span class="p">})</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>Then some tests:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[cfg(test)]</span>
<span class="k">mod</span> <span class="n">tests</span> <span class="p">{</span>
    <span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">path</span><span class="p">::</span><span class="n">PathBuf</span><span class="p">;</span>
    <span class="k">use</span> <span class="k">super</span><span class="p">::</span><span class="o">*</span><span class="p">;</span>

    <span class="nd">#[test]</span>
    <span class="k">fn</span> <span class="nf">parse_test_network_xml</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">let</span> <span class="k">mut</span> <span class="n">path</span> <span class="o">=</span> <span class="nn">PathBuf</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="nd">env!</span><span class="p">(</span><span class="s">"CARGO_MANIFEST_DIR"</span><span class="p">));</span>
        <span class="n">path</span><span class="nf">.push</span><span class="p">(</span><span class="s">"fixtures/network_test.xml"</span><span class="p">);</span>
        <span class="k">let</span> <span class="n">links</span> <span class="o">=</span> <span class="nn">Links</span><span class="p">::</span><span class="nf">from_xml</span><span class="p">(</span><span class="n">path</span><span class="p">,</span> <span class="k">false</span><span class="p">)</span><span class="nf">.unwrap</span><span class="p">();</span>
        <span class="nd">assert_eq!</span><span class="p">(</span><span class="n">links</span><span class="py">.lengths</span><span class="nf">.len</span><span class="p">(),</span> <span class="mi">2</span><span class="p">);</span>
        <span class="nd">assert_eq!</span><span class="p">(</span><span class="n">links</span><span class="py">.lengths</span><span class="nf">.get</span><span class="p">(</span><span class="s">"linkE"</span><span class="p">),</span> <span class="nf">Some</span><span class="p">(</span><span class="o">&amp;</span><span class="mf">100.5</span><span class="p">));</span>
        <span class="nd">assert_eq!</span><span class="p">(</span><span class="n">links</span><span class="py">.lengths</span><span class="nf">.get</span><span class="p">(</span><span class="s">"linkW"</span><span class="p">),</span> <span class="nf">Some</span><span class="p">(</span><span class="o">&amp;</span><span class="mf">102.0</span><span class="p">));</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/work-480.webp 480w,/assets/img/work-800.webp 800w,/assets/img/work-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/work.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <a href="https://xkcd.com/1741">xkcd</a> </div> <h1 id="benchmarking">Benchmarking</h1> <p>Reminder that I want to build two types of benchmark:</p> <ul> <li>how fast can we parse the xml</li> <li>how fast can we access link lengths from our <code class="language-plaintext highlighter-rouge">Links</code> struct</li> </ul> <p>I’m using <code class="language-plaintext highlighter-rouge">criterion</code> for benchmarking. I am not going to go into details (criterion has a good getting started) just focus on broad sweeps.</p> <h2 id="parsing-xml-1">Parsing XML</h2> <p>Easy peasy, I bosh a big example xml in memory. I am slightly concerned that using too small a dataset will make measuring improvemnts hard because there will be some fixed io cost that will dominate tiny examples. So I end up using an 8MB MATSim network from central London.</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">fn</span> <span class="nf">parse_link_lengths</span><span class="p">(</span><span class="n">c</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">mut</span> <span class="n">Criterion</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">c</span><span class="nf">.bench_function</span><span class="p">(</span><span class="s">"build link lengths from xml"</span><span class="p">,</span> <span class="p">|</span><span class="n">b</span><span class="p">|</span> <span class="p">{</span>
        <span class="n">b</span><span class="nf">.iter</span><span class="p">(||</span> <span class="p">{</span>
            <span class="k">let</span> <span class="k">mut</span> <span class="n">path</span> <span class="o">=</span> <span class="nn">PathBuf</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="nd">env!</span><span class="p">(</span><span class="s">"CARGO_MANIFEST_DIR"</span><span class="p">));</span>
            <span class="n">path</span><span class="nf">.push</span><span class="p">(</span><span class="s">"fixtures/network_big.xml"</span><span class="p">);</span>
            <span class="k">let</span> <span class="n">links</span> <span class="o">=</span> <span class="nn">Links</span><span class="p">::</span><span class="nf">from_xml</span><span class="p">(</span><span class="nf">black_box</span><span class="p">(</span><span class="n">path</span><span class="p">))</span><span class="nf">.unwrap</span><span class="p">();</span>
        <span class="p">})</span>
    <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div> <p>We can load this network in ~30ms.</p> <h2 id="accessing">Accessing</h2> <p>This is basically just calling <code class="language-plaintext highlighter-rouge">get(&amp;key)</code> on <code class="language-plaintext highlighter-rouge">LinkLengths</code> which is a hashmap. I use the same network for this test but keep it’s creation outside the benchmarker. I stress a little about having a systematic and consistent way of creating link keys:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">fn</span> <span class="nf">access_link_lengths</span><span class="p">(</span><span class="n">c</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">mut</span> <span class="n">Criterion</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">path</span> <span class="o">=</span> <span class="nn">PathBuf</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="nd">env!</span><span class="p">(</span><span class="s">"CARGO_MANIFEST_DIR"</span><span class="p">));</span>
    <span class="n">path</span><span class="nf">.push</span><span class="p">(</span><span class="s">"fixtures/network_big.xml"</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">links</span> <span class="o">=</span> <span class="nn">Links</span><span class="p">::</span><span class="nf">from_xml</span><span class="p">(</span><span class="n">path</span><span class="p">,</span> <span class="k">false</span><span class="p">)</span><span class="nf">.unwrap</span><span class="p">();</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">rng</span> <span class="o">=</span> <span class="nn">StdRng</span><span class="p">::</span><span class="nf">seed_from_u64</span><span class="p">(</span><span class="mi">1234</span><span class="p">);</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">link_keys</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="n">_</span><span class="o">&gt;</span> <span class="o">=</span> <span class="p">(</span><span class="mi">0</span><span class="o">..</span><span class="n">links</span><span class="py">.lengths</span><span class="nf">.len</span><span class="p">())</span><span class="py">.collect</span><span class="p">::</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="n">_</span><span class="o">&gt;&gt;</span><span class="p">();</span>
    <span class="n">link_keys</span><span class="nf">.sort</span><span class="p">();</span>
    <span class="k">let</span> <span class="n">samples</span> <span class="o">=</span> <span class="nf">black_box</span><span class="p">(</span><span class="n">link_keys</span><span class="nf">.iter</span><span class="p">()</span><span class="nf">.choose_multiple</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">rng</span><span class="p">,</span> <span class="mi">1000</span><span class="p">));</span>

    <span class="n">c</span><span class="nf">.bench_function</span><span class="p">(</span><span class="s">"query link lengths"</span><span class="p">,</span> <span class="p">|</span><span class="n">b</span><span class="p">|</span> <span class="p">{</span>
        <span class="n">b</span><span class="nf">.iter</span><span class="p">(||</span> <span class="p">{</span>
            <span class="k">for</span> <span class="n">lid</span> <span class="k">in</span> <span class="o">&amp;</span><span class="n">samples</span> <span class="p">{</span>
                <span class="n">links</span><span class="nf">.get</span><span class="p">(</span><span class="o">**</span><span class="n">lid</span><span class="p">);</span>
            <span class="p">}</span>
        <span class="p">})</span>
    <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div> <p>We can access each length in about 3µs.</p> <h1 id="fiddling">Fiddling</h1> <h2 id="quick_xml-checks">Quick_xml Checks</h2> <p>Disabling the quick_xml checks (<code class="language-plaintext highlighter-rouge">let mut iter_attributes = attributes.with_checks(false).flatten();</code>) turns out to save us 5% loading time - which seems well worth any risk. So we keep the checks removed.</p> <h2 id="precision">Precision</h2> <p>f32 is probably overkill. We can reasonably expect links to be between several meters and several kms long. We can also expect them to always be positive. We also might not care much about decimal places. So I check the impact of switching to u16 (unsigned integer with maximum around 65k).</p> <p>No change to our benchmarks. This is expected. We actually have to still parse the xml lengths as floats before converting them to integers (maybe we could to something better).</p> <p>We do know that we have halved the link lengths memory requirement (at least in theory) and can get faster subsequent operations with integers. But it is at the expense of precision, which might cause problems later on. For example I know we will need to calculate duration on a link for a given speed. This will inneviatbly be a float operation and for short links, precision will matter.</p> <p>So we could try f16 from <a href="https://docs.rs/half/latest/half/index.html">half</a>. But reading the docs I see that we can only guarantee memory improvements, as not all hardware will support f16 operations. But we’ve come this far and so i set up a new benchmarks that extracts the link lengths and then makes some arbitrary operations. This turns out to be about 5% slower using half::f16, presumably as it adds a bunch of conversions on my hardware.</p> <p>So to summarise, f32 seems fine for now. If it turns out that precision doesn’t matter as much but memory does. Then there are some easy memory wins, but they might come at the expense of inprecise or slower ops.</p> <h2 id="strings">Strings</h2> <p>We need to create some data in memory for each key (the fast_xml buffer from which we parse the key is transient). Using <code class="language-plaintext highlighter-rouge">String</code> is not great. We immediately have to allocate to heap and we get some functionality (<code class="language-plaintext highlighter-rouge">.push()</code>) that we don’t need - our keys can be immutable.</p> <p><a href="https://docs.rs/compact_str/0.1.0/compact_str/struct.CompactStr.html">compact_str</a> has <em>“an immutable a memory efficient immuatable string that can be used almost anywhere a String or &amp;str can be used”.</em> Sounds good but seems to cause a 5% regression for our hashmap accessing benchmark. Not really sure why but I’m expecting to hit the hashmap a lot so no good.</p> <h2 id="btreemap">BTreeMap</h2> <p>Replacing the HashMap with a BTreeMap very bad idea. Writting is 5% slower. Access 300% slower.</p> <h2 id="complete-reindex">Complete reindex</h2> <p>I already know that I am going to be looking up link lengths (and other values) a lot. The fastest way to do this would be to dump the length values into an array and use a <code class="language-plaintext highlighter-rouge">usize</code> key to directly access the array.</p> <p>To be actually useful I add an <code class="language-plaintext highlighter-rouge">Indexer</code>, which provides methods to (i) consistently map keys into <code class="language-plaintext highlighter-rouge">usize</code> (the same key should always return the same <code class="language-plaintext highlighter-rouge">usize</code>), and (ii) I need to be able to retrieve my keys again using the new <code class="language-plaintext highlighter-rouge">usize</code>:</p> <div class="language-rs highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">collections</span><span class="p">::</span><span class="n">HashMap</span><span class="p">;</span>

<span class="nd">#[derive(Debug,</span> <span class="nd">Default)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">Indexer</span> <span class="p">{</span>
    <span class="n">key_to_index</span><span class="p">:</span> <span class="n">HashMap</span><span class="o">&lt;</span><span class="nb">String</span><span class="p">,</span> <span class="nb">usize</span><span class="o">&gt;</span><span class="p">,</span>
    <span class="n">index_to_key</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span>
<span class="p">}</span>

<span class="k">impl</span> <span class="n">Indexer</span> <span class="p">{</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">new</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="k">Self</span> <span class="p">{</span>
        <span class="nn">Indexer</span><span class="p">::</span><span class="nf">default</span><span class="p">()</span>
    <span class="p">}</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">add</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="k">self</span><span class="p">,</span> <span class="n">key</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">let</span> <span class="n">index</span> <span class="o">=</span> <span class="k">self</span><span class="py">.index_to_key</span><span class="nf">.len</span><span class="p">();</span>
        <span class="c1">// todo - remove clone</span>
        <span class="k">self</span><span class="py">.key_to_index</span><span class="nf">.insert</span><span class="p">(</span><span class="n">key</span><span class="nf">.clone</span><span class="p">(),</span> <span class="n">index</span><span class="p">);</span>
        <span class="k">self</span><span class="py">.index_to_key</span><span class="nf">.push</span><span class="p">(</span><span class="n">key</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">get_key</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">index</span><span class="p">:</span> <span class="nb">usize</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="c1">// todo - has a clone - prefer to return a ref</span>
        <span class="k">if</span> <span class="n">index</span> <span class="o">&lt;=</span> <span class="k">self</span><span class="py">.index_to_key</span><span class="nf">.len</span><span class="p">()</span> <span class="p">{</span>
            <span class="k">return</span> <span class="nb">None</span>
        <span class="p">}</span>
        <span class="nf">Some</span><span class="p">(</span><span class="k">self</span><span class="py">.index_to_key</span><span class="p">[</span><span class="n">index</span><span class="p">]</span><span class="nf">.clone</span><span class="p">())</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>At xml read time this builds a map of keys to <code class="language-plaintext highlighter-rouge">usize</code> plus puts a clone of the keys in a vec so they can be retrieved using the index. This is a little more work and about double the memory, but only causes a 5% regression on xml read time. We can probably engineer out the clone and fuss around with the String size in future.</p> <p>Accessing link lengths using the new <code class="language-plaintext highlighter-rouge">usize</code> is obviously a lot faster (~95% faster), we no longer have to worry about the hash, instead we immediately fetch with the vec index. Provided we are going to be doing more a lot more accessing versus creating then this is a no-brainer.</p> <hr/> <p><strong>NOTE</strong></p> <p>WIP ___</p>]]></content><author><name></name></author><category term="Dev"/><category term="MATSim"/><category term="Rust"/><category term="parsing"/><category term="xml"/><summary type="html"><![CDATA[a Rust parser for MATSim network format]]></summary></entry><entry><title type="html">Introducing MC: An Open-Source Tool for Configuring MATSim</title><link href="https://fredshone.github.io/blog/2022/introducing-mc-an-open-source-tool-for-configuring-matsim/" rel="alternate" type="text/html" title="Introducing MC: An Open-Source Tool for Configuring MATSim"/><published>2022-01-21T11:16:52+00:00</published><updated>2022-01-21T11:16:52+00:00</updated><id>https://fredshone.github.io/blog/2022/introducing-mc-an-open-source-tool-for-configuring-matsim</id><content type="html" xml:base="https://fredshone.github.io/blog/2022/introducing-mc-an-open-source-tool-for-configuring-matsim/"><![CDATA[]]></content><author><name></name></author></entry><entry><title type="html">Pandemic Activity Modifier (Part 2): Activity Plans</title><link href="https://fredshone.github.io/blog/2020/pandemic-activity-modifier-part-2-activity-plans/" rel="alternate" type="text/html" title="Pandemic Activity Modifier (Part 2): Activity Plans"/><published>2020-05-20T08:38:02+00:00</published><updated>2020-05-20T08:38:02+00:00</updated><id>https://fredshone.github.io/blog/2020/pandemic-activity-modifier-part-2-activity-plans</id><content type="html" xml:base="https://fredshone.github.io/blog/2020/pandemic-activity-modifier-part-2-activity-plans/"><![CDATA[]]></content><author><name></name></author></entry><entry><title type="html">Pandemic Activity Modifier: Intro</title><link href="https://fredshone.github.io/blog/2020/pandemic-activity-modifier-intro/" rel="alternate" type="text/html" title="Pandemic Activity Modifier: Intro"/><published>2020-05-15T15:53:32+00:00</published><updated>2020-05-15T15:53:32+00:00</updated><id>https://fredshone.github.io/blog/2020/pandemic-activity-modifier-intro</id><content type="html" xml:base="https://fredshone.github.io/blog/2020/pandemic-activity-modifier-intro/"><![CDATA[]]></content><author><name></name></author></entry><entry><title type="html">City Chef: Bring Your Own City (BYOC)</title><link href="https://fredshone.github.io/blog/2020/city-chef-bring-your-own-city-byoc/" rel="alternate" type="text/html" title="City Chef: Bring Your Own City (BYOC)"/><published>2020-04-03T09:22:06+00:00</published><updated>2020-04-03T09:22:06+00:00</updated><id>https://fredshone.github.io/blog/2020/city-chef-bring-your-own-city-byoc</id><content type="html" xml:base="https://fredshone.github.io/blog/2020/city-chef-bring-your-own-city-byoc/"><![CDATA[]]></content><author><name></name></author></entry></feed>